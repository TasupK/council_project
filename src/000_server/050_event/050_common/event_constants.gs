/**
 * 행사복지관리 서비스 공통 상수.
 * DB 스키마에 없는 화면용 값은 이 파일에서만 관리한다.
 */

var EVENT_STATUSES = ['예정', '모집', '진행', '종료', '취소'];
var EVENT_ATTENDANCE_STATUSES = ['출석', '미참석'];
var EVENT_DEFAULT_PAGE_SIZE = 10;
var EVENT_MAX_PAGE_SIZE = 100;
